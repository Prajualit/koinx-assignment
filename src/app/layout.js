import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Koinx",
  description: "Welcome to the KoinX Assignment project! This repository contains a cryptocurrency tracking application built with modern tools like Next.js, React, and the CoinGecko API. It provides users with insights into trending cryptocurrencies, live market data, and detailed analysis, all wrapped in a responsive, user-friendly interface.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
